{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "TaskFlow API — Node.js + SQLite",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/seed.js",
    "test": "jest --runInBand --forceExit"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "better-sqlite3": "*",
    "cors": "*",
    "express": "*",
    "express-validator": "*",
    "helmet": "*"
  },
  "devDependencies": {
    "jest": "*",
    "nodemon": "*",
    "supertest": "*"
  }
}
